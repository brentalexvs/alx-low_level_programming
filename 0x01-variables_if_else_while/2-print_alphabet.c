#include <stdio.h>

/**
 * main - entry point
 *
 * This function is to display the alphabet
 *
 * Return - always 0
 */

int main(void)
{
	for (a = 'a' ; a <= 'z' ; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
