#include <stdio.h>

/**
 * main - print single digits from bas 10 to 0
 *
 * Return: always 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10 ; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
