#include <stdio.h>

/**
 * main - print the programm name
 * @argc: the arugment counter
 * @argv: the argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
